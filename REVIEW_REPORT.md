# Codeforces Helper 审查记录

> 此文件保留修复前的审查快照。2026-09-07 的修复结果与当前验收入口见 [ACCEPTANCE_REPORT.md](ACCEPTANCE_REPORT.md)。`review/2026-09-06/reproduce.py` 针对旧版接口保留作历史证据，当前回归测试位于 `tests/`。

审查日期：2026-09-06。版本：1.3.1。基准提交：`20a6c172a0e7d6ca29d44c357ddd1835761d8f6c`。

本轮检查功能正确性、数据一致性、平台兼容性和 Web 管理流程，未修改业务代码。

## 验证范围

- 仓库自带测试：`python -m unittest discover -s tests -v`，33 项全部通过。
- 另用真实 Quart、aiosqlite、SQLite 和原始插件方法进行了针对性复现；10 项预期行为断言均失败。其中同一个“短天数同步跳过缺口”问题分别验证了主插件和 Web 后台两个入口。
- 独立执行 `python -c "import core.sync_lock"`，在本机 Windows/Python 3.10.10 上直接得到 `ModuleNotFoundError: No module named 'fcntl'`。
- 使用原始网页、临时数据库及仅监听 `127.0.0.1` 的服务，在浏览器中确认存储型脚本注入。验证只显示无害文字，测试服务及页面已关闭。
- 实际请求 Codeforces `user.info`，同时传入 `tourist` 和测试用不存在 Handle，得到 HTTP 400、`FAILED`、`User with handle ... not found`，确认单个无效成员会导致整批 API 失败。
- 没有部署完整 AstrBot/QQ 运行环境，没有发送 QQ 消息。独立复现替换了 AstrBot 宿主、网络响应和 Windows 上不可用的 Unix 文件锁；不据此宣称真实 QQ 接入、热重载或跨进程锁已通过验证。

复现文件：[reproduce.py](review/2026-09-06/reproduce.py)、[结果 JSON](review/2026-09-06/results.json)、[浏览器证据](review/2026-09-06/browser-proof.json)。

在本次创建的隔离环境中运行：

```powershell
.\.venv\Scripts\python.exe -X utf8 review\2026-09-06\reproduce.py
```

脚本退出码 1 表示预期正确行为尚未满足，符合当前未修复版本的复现结果。数据库均在临时目录中创建并清理。

## 建议优先修复的问题

### 1. 增量同步会永久漏掉稍后才判为 AC 的提交

- 位置：`core/crawler.py:130-135`、`main.py:473-479`、`backend/api.py:83-86`。
- 触发：提交时间为 `T-1`；`T` 时同步看到 `TESTING`，跳过此提交但仍把游标推进到 `T`；下一轮返回 `OK`，却因提交时间早于游标而继续被跳过。
- 实测：两轮同步后应有 1 条 AC，实际为 0，普通增量同步不会自动补回。
- 修复方向：记录待判提交并复查；增量回看应有重叠窗口，并为超出窗口的重判保留独立补偿机制。不要把“请求成功”等同于“时间段内所有提交判决已稳定”。

### 2. 手动同步较短天数会跳过尚未覆盖的历史区间

- 位置：`main.py:454-479`、`backend/api.py:70-86`。
- 触发：上次成功同步是 10 天前，成员已有 30 天补全标记；管理员执行 1 天深度同步。代码仅抓 1 天，却把增量游标直接改为现在。
- 实测：5 天前的一条 AC，在随后普通同步中仍未入库；主插件和后台两个入口都复现。
- 修复方向：手动历史补抓和增量游标分开维护，或同时从旧游标补齐至现在，确认覆盖连续后才推进游标。

### 3. 同步与定时播报错开时，已入库记录仍可能永远漏报

- 位置：`main.py:431-436`、`main.py:506-507`，调度入口为 `main.py:192-215`。
- 触发：同步在每小时 10 分执行，播报在整点执行；12:30 的 AC 在 13:10 才入库。13:00 时还查不到它，14:00 只查询 13:00 后的提交，又把它排除。
- 实测：数据库最终有该 AC，但相邻两次应覆盖它的播报都不包含它。
- 修复方向：持久化“已成功播报”的记录或游标，按未播报记录发送；成功后再确认。仅调整两个定时器先后顺序仍不能覆盖同步失败或延迟。

### 4. 成员输入可形成存储型脚本注入

- 位置：`public/index.html:50`、`public/index.html:53`；`public/index.html:65` 的属性插值也使用同一函数。成员保存入口为 `backend/api.py:250-257`。
- 原因：`esc()` 使用 `textContent` 转 `innerHTML`，能处理 HTML 文本中的标签符号，却不会替 HTML 属性编码双引号。Handle 随后被拼进 `title="..."`。
- 实测：通过管理员成员接口保存含引号和事件属性的测试 Handle，公开排行榜生成了真实 `onmouseover` 属性；鼠标移入后页面出现 `LOCAL_REVIEW_PROOF`。
- 前提：恶意内容需要先经管理员保存或导入。未发现匿名用户能直接写入成员资料，不能据此宣称匿名攻击者可直接修改数据库。
- 修复方向：用 DOM API 设置 `textContent`、`title`、`href` 等属性，避免把资料拼接进 HTML；后端同时校验 Handle 格式。仅对 URL 执行 `encodeURIComponent` 无法保护另一个 `title` 属性。

### 5. Windows 环境无法加载插件

- 位置：`core/sync_lock.py:1`。该模块被 `main.py` 和 Web 后台导入。
- 实测：模块导入即因缺少 `fcntl` 失败，还未进入插件初始化。
- 修复方向：提供跨平台进程锁实现并验证互斥；不能简单在 Windows 上跳过加锁。若仅打算支持 Linux，应明确运行系统限制。
- 现有测试中的 `fcntl` 替身会掩盖此加载问题。

## 其他已复现的数据问题

### 6. 更换 Handle 后保留了旧账号 Rating

- 位置：`backend/api.py:263-272`。
- 现状：Handle 改变时清除过题记录和同步游标，却没有清空 `cf_rating`、`cf_rank`、`cf_max_rating`、`cf_max_rank`、`cf_rating_updated_at`。
- 实测：旧账号 2500/2700 分，改成另一个 Handle 后仍为 2500/2700 分。即使把 Handle 清空也会残留旧缓存，直到正确处理或后续成功刷新。
- 修复方向：在同一事务中清空全部账号相关缓存并标记待更新。

### 7. 一个无效 Handle 会阻断全部成员的 Rating 刷新

- 位置：`core/crawler.py:25-37`、`main.py:499`、`backend/api.py:91`。
- 实测：CF 官方接口对混有无效 Handle 的请求整批返回 HTTP 400；插件没有拆批或逐个回退。复现中有效成员应更新至 1900，实际继续保留 2500。
- 修复方向：保存成员时验证；批量失败时拆分定位无效账号，继续更新其他成员；在后台单独显示失败 Handle 和错误原因。

### 8. 多个 QQ 绑定同一 Handle 时只有最后一个更新 Rating

- 位置：`core/crawler.py:25`、`core/crawler.py:39-45`。
- 原因：`handle_to_qq` 是一对一字典，后一个 QQ 会覆盖前一个；数据库和成员接口却允许多个 QQ 绑定同一 Handle。
- 实测：两名成员绑定相同 Handle，批量刷新后分别为 2500、1900，而不是都为 1900。
- 修复方向：明确产品规则；允许共享时维护 Handle 到 QQ 列表，并去重 API 参数；禁止共享时增加忽略大小写的唯一性校验与友好提示。

### 9. 同一份提交历史会因导入顺序不同产生不同周榜

- 位置：`core/crawler.py:139-150`、`core/crawler.py:164-169`。
- 触发：同一题 10 天前和昨天均 AC。持续同步的数据库保存 10 天前的记录，昨天的重复 AC 被唯一约束忽略；新数据库一次回查则优先保存昨天的 AC。
- 实测：同样的 API 历史，持续同步周榜计数为 0，首次回查为 1。
- 修复方向：明确统计首次 AC 还是周期内练习；如统计首次 AC，需要按题取最早时间，并允许深度回查修正已有时间；如统计周期内练习，需要保留原始提交，再按查询周期去重。

### 10. 已记为 AC 的提交后来失效，深度同步也不会纠正

- 位置：`core/crawler.py:134-135`、`core/crawler.py:164-169`。
- 实测：首次返回 `OK` 入库；后续返回 `CHALLENGED`，即使执行 30 天深度同步，原 AC 仍存在。
- 修复方向：保留提交 ID、最终判决和比赛状态，定期复核比赛结束后的记录；某题仍有其他有效 AC 时应继续保留过题状态，不能看到一个失败提交就删除整题。

## 会话管理建议

修改密码后，其他已登录会话仍保持管理员权限。

- 位置：`backend/api.py:174-177`、`backend/api.py:216-231`，会话鉴权为 `backend/api.py:27-31`。
- 实测：两个独立客户端登录，其中一个修改密码，另一个仍可对 `/api/admin/users` 获得 HTTP 200。
- 这是已确认的安全设计缺口；当前产品没有“修改密码退出所有设备”的明示承诺，因此与明确的功能错误分开列出。
- 建议：维护会话版本或密码变更时间并在鉴权时比对，修改密码后吊销旧会话；可让当前设备重新登录。

## 可以增加的内容

建议按“先保证数据可信，再增加训练价值”的顺序安排。

| 优先级 | 内容 | 对用户的价值 | 主要工作量 |
| --- | --- | --- | --- |
| 优先 | 同步任务中心 | 显示进度、失败成员、最后成功时间、覆盖日期和失败重试；避免把部分失败显示为笼统的完成 | 中 |
| 优先 | 成员自助绑定与验证 | `/acm bind`、解绑、查询自己，减少管理员手工导入；须验证账号归属 | 中 |
| 优先 | 赛后群内战报 | 展示群成员比赛名次、解题数和正式 Rating 涨跌；支持成绩迟发布后的补发和去重 | 中 |
| 后续 | 按难度和标签推荐未做题 | 支持每日一题、训练目标、指定标签，并排除已 AC 题 | 中 |
| 后续 | 个人训练画像 | Rating 曲线、难度分布、标签分布、周报和连续训练记录；标签/历史信息需补充数据存储 | 中 |
| 后续 | 多群独立配置 | 各群独立成员范围、排行榜、播报时间和比赛订阅，适合多个班级或队伍 | 中至高 |
| 配套 | 备份、导入预览和统计口径说明 | 成员批量修改前展示变化；提示 Handle 切换会清记录；总榜显示本地历史覆盖范围 | 低至中 |

工程上建议补充真实 SQLite/Quart 集成测试、Windows/Linux 导入检查和基础 CI，覆盖判题状态变化、同步游标、批量失败隔离、播报补偿及前端输入处理。现有部分测试只验证源码片段或依赖替身，因此 33 项通过不能覆盖这些真实故障。

## 核对的外部依据

- [Codeforces API 对象说明](https://codeforces.com/apiHelp/objects#Submission)：提交存在 `TESTING`、`SUBMITTED`、`CHALLENGED` 等状态，`creationTimeSeconds` 是提交创建时间。
- [Codeforces API 方法说明](https://codeforces.com/apiHelp/methods#user.info)：批量用户资料、提交历史和 Rating 历史接口；复现额外确认了无效 Handle 的整批失败行为。
- [Codeforces API 调用限制](https://codeforces.com/apiHelp)：扩展同步和赛后功能时应继续复用当前共享限流器。
- [AstrBot 命令过滤实现](https://github.com/AstrBotDevs/AstrBot/blob/master/astrbot/core/star/filter/command.py)：已核对当前源码，不把命令名包含空格本身判为已确认 bug；完整 QQ 命令路由仍未做宿主集成验证。
